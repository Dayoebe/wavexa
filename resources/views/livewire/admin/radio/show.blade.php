<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif
    @php($artwork=$station->artworks->firstWhere('is_primary',true)?->url)
    <div class="flex flex-col gap-5 sm:flex-row sm:items-start sm:justify-between"><div class="flex gap-4"><span class="relative grid size-20 shrink-0 place-items-center overflow-hidden rounded-2xl bg-orange-100 text-3xl font-extrabold text-orange-700">{{ mb_strtoupper(mb_substr(ltrim($station->name,'#'),0,1)) }}@if($artwork)<img src="{{ $artwork }}" alt="{{ $station->name }} logo" class="absolute inset-0 size-full object-contain" onerror="this.remove()">@endif</span><div><a wire:navigate href="{{ route('admin.radio.index') }}" class="text-xs font-bold text-slate-500">← Radio catalogue</a><h2 class="mt-2 text-3xl font-extrabold">{{ $station->name }}</h2><div class="mt-2 flex flex-wrap gap-2"><span class="rounded-full bg-emerald-100 px-2.5 py-1 text-[9px] font-extrabold uppercase text-emerald-800">{{ $station->status->value }}</span><span class="rounded-full bg-stone-200 px-2.5 py-1 text-[9px] font-bold">ID {{ $station->id }}</span></div></div></div><div class="flex flex-wrap gap-2"><button type="button" data-play-station data-title="{{ $station->name }}" data-slug="{{ $station->slug }}" data-art="{{ mb_strtoupper(mb_substr(ltrim($station->name,'#'),0,1)) }}" data-stream="{{ $station->primaryStream?->resolved_url ?: $station->primaryStream?->url }}" data-streams="{{ $station->streamSources->where('status','!=',\App\Enums\StreamStatus::Offline)->map(fn($stream)=>['id'=>$stream->id,'url'=>$stream->resolved_url?:$stream->url,'format'=>$stream->format])->values()->toJson() }}" class="rounded-xl bg-orange-600 px-5 py-3 text-xs font-extrabold text-white">▶ Play live</button><a wire:navigate href="{{ route('radio.show',$station->slug) }}" target="_blank" class="rounded-xl border border-stone-300 bg-white px-4 py-3 text-xs font-bold">Public page ↗</a><a wire:navigate href="{{ route('admin.radio.edit',$station) }}" class="rounded-xl bg-blue-600 px-5 py-3 text-xs font-extrabold text-white">Edit station</a></div></div>
        <div class="mt-7 grid gap-6 xl:grid-cols-[1fr_360px]"><div class="space-y-6"><section class="rounded-2xl border border-stone-200 bg-white p-6"><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-orange-600">Station details</p><dl class="mt-5 grid gap-5 sm:grid-cols-2"><div><dt class="text-xs font-bold text-slate-400">Description</dt><dd class="mt-1 leading-6 sm:col-span-2">{{ $station->description ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Website</dt><dd class="mt-1 break-all font-semibold">{{ $station->website_url ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Slug</dt><dd class="mt-1 font-semibold">{{ $station->slug }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Call sign</dt><dd class="mt-1 font-semibold">{{ $station->radioStation?->call_sign ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Frequency</dt><dd class="mt-1 font-semibold">{{ $station->radioStation?->frequency ? $station->radioStation->frequency.' '.$station->radioStation->frequency_unit : 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Location</dt><dd class="mt-1 font-semibold">{{ collect([$station->city?->name,$station->administrativeArea?->name,$station->country?->name])->filter()->join(', ') ?: 'Not assigned' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Provider region</dt><dd class="mt-1 font-semibold">{{ $station->radioStation?->source_state ?: 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Coordinates</dt><dd class="mt-1 font-semibold">{{ $station->radioStation?->latitude && $station->radioStation?->longitude ? $station->radioStation->latitude.', '.$station->radioStation->longitude : 'Not supplied' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Categories</dt><dd class="mt-1 font-semibold">{{ $station->categories->pluck('name')->join(', ') ?: 'Not assigned' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Genres</dt><dd class="mt-1 font-semibold">{{ $station->genres->pluck('name')->join(', ') ?: 'Not assigned' }}</dd></div><div><dt class="text-xs font-bold text-slate-400">Languages</dt><dd class="mt-1 font-semibold">{{ $station->languages->pluck('name')->join(', ') ?: 'Not assigned' }}</dd></div></dl></section>
        <section class="rounded-2xl border border-stone-200 bg-white p-6"><div class="flex items-end justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-emerald-700">Playback</p><h3 class="mt-1 text-xl font-extrabold">Stream sources</h3></div><span class="text-xs text-slate-400">{{ $station->streamSources->count() }} sources</span></div><div class="mt-5 space-y-3">@forelse($station->streamSources as $stream)<article class="rounded-xl border border-stone-200 bg-stone-50 p-4"><div class="flex flex-wrap items-start justify-between gap-3"><div class="min-w-0"><div class="flex gap-2"><span class="rounded-full bg-slate-950 px-2 py-1 text-[9px] font-extrabold uppercase text-white">{{ $stream->format }}</span>@if($stream->is_primary)<span class="rounded-full bg-orange-100 px-2 py-1 text-[9px] font-extrabold text-orange-800">Primary</span>@endif<span class="rounded-full bg-white px-2 py-1 text-[9px] font-bold">{{ $stream->status->value }}</span></div><p class="mt-3 break-all text-xs font-semibold">{{ $stream->url }}</p><p class="mt-1 text-[10px] text-slate-400">{{ $stream->sourceProvider?->name ?? 'Manual source' }} · {{ $stream->codec ?: 'Unknown codec' }} · {{ $stream->bitrate_kbps ? $stream->bitrate_kbps.' kbps' : 'Unknown bitrate' }}</p></div><span class="text-[10px] font-bold uppercase text-slate-400">{{ $stream->verification_status->value }}</span></div><dl class="mt-4 grid grid-cols-2 gap-3 border-t border-stone-200 pt-3 text-xs sm:grid-cols-4"><div><dt class="text-slate-400">HTTP</dt><dd class="font-bold">{{ $stream->http_status ?? '—' }}</dd></div><div><dt class="text-slate-400">Response</dt><dd class="font-bold">{{ $stream->response_time_ms ? $stream->response_time_ms.'ms' : '—' }}</dd></div><div><dt class="text-slate-400">Failures</dt><dd class="font-bold">{{ $stream->failure_count }}</dd></div><div><dt class="text-slate-400">Reports</dt><dd class="font-bold">{{ $stream->reports->whereNull('resolved_at')->count() }}</dd></div></dl></article>@empty<p class="rounded-xl bg-amber-50 p-5 text-sm font-semibold text-amber-900">No stream sources have been added.</p>@endforelse</div></section></div>
        <aside class="space-y-6"><section class="rounded-2xl bg-slate-950 p-6 text-white"><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-cyan-300">Directory signals</p><dl class="mt-5 space-y-4 text-sm"><div class="flex justify-between"><dt class="text-slate-400">Votes</dt><dd class="font-bold">{{ number_format($station->radioStation?->source_vote_count ?? 0) }}</dd></div><div class="flex justify-between"><dt class="text-slate-400">Clicks</dt><dd class="font-bold">{{ number_format($station->radioStation?->source_click_count ?? 0) }}</dd></div><div class="flex justify-between"><dt class="text-slate-400">Trend</dt><dd class="font-bold">{{ number_format($station->radioStation?->source_click_trend ?? 0) }}</dd></div><div class="flex justify-between"><dt class="text-slate-400">Changed</dt><dd class="font-bold">{{ $station->radioStation?->source_changed_at?->diffForHumans() ?? 'Unknown' }}</dd></div></dl></section><section class="rounded-2xl border border-stone-200 bg-white p-6"><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-violet-700">Source provenance</p><div class="mt-4 space-y-4">@forelse($station->sources as $source)<div><strong class="text-sm">{{ $source->sourceProvider?->name }}</strong><p class="mt-1 break-all text-xs text-slate-500">{{ $source->external_identifier ?: 'No external ID' }}</p><p class="mt-1 text-[10px] text-slate-400">Imported {{ $source->imported_at?->diffForHumans() }}</p></div>@empty<p class="text-sm text-slate-500">Manually managed station.</p>@endforelse</div></section><section class="rounded-2xl border border-stone-200 bg-white p-6 text-xs text-slate-500"><p>Created {{ $station->created_at->format('M j, Y · H:i') }}</p><p class="mt-2">Updated {{ $station->updated_at->format('M j, Y · H:i') }}</p></section></aside></div>
</div>
