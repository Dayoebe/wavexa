<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-amber-700">Sources & playback · Policy</p><h2 class="mt-2 text-3xl font-extrabold">Geoblocking</h2><p class="mt-2 max-w-2xl text-sm text-slate-500">Create country allowlists or blocklists per stream. Wavexa evaluates trusted edge-country headers before playback.</p></div><section class="mt-7 rounded-3xl border border-stone-200 bg-white p-5"><h3 class="text-lg font-extrabold">Add availability rule</h3><input wire:model.live.debounce.300ms="q" class="mt-4 min-h-12 w-full rounded-xl border-stone-300 bg-stone-50" placeholder="Search media to narrow stream list"><div class="mt-3 grid gap-3 md:grid-cols-[1fr_240px_160px_auto]"><select wire:model="streamId" class="min-h-12 rounded-xl border-stone-300"><option value="">Choose stream</option>@foreach($streams as $stream)<option value="{{ $stream->id }}">{{ $stream->media?->name }} · {{ strtoupper($stream->format) }}</option>@endforeach</select><select wire:model="countryId" class="min-h-12 rounded-xl border-stone-300"><option value="">Choose country</option>@foreach($countries as $country)<option value="{{ $country->id }}">{{ $country->name }} ({{ $country->iso_alpha_2 }})</option>@endforeach</select><select wire:model="mode" class="min-h-12 rounded-xl border-stone-300"><option value="blocked">Block country</option><option value="allowed">Allow only</option></select><button wire:click="save" class="min-h-12 rounded-xl bg-slate-950 px-5 text-sm font-bold text-white">Save rule</button></div></section><div class="mt-5 grid gap-3">@forelse($rules as $rule)<article class="flex items-center gap-3 rounded-2xl border border-stone-200 bg-white p-4"><span class="grid size-11 place-items-center rounded-xl {{ $rule->mode==='blocked'?'bg-rose-50 text-rose-700':'bg-emerald-50 text-emerald-700' }} font-black">{{ $rule->country->iso_alpha_2 }}</span><div class="min-w-0 flex-1"><strong>{{ $rule->streamSource?->media?->name }}</strong><p class="text-xs text-slate-500">{{ str($rule->mode)->headline() }} in {{ $rule->country->name }}</p></div><button wire:click="delete({{ $rule->id }})" class="rounded-xl bg-rose-50 px-3 py-2 text-xs font-bold text-rose-700">Remove</button></article>@empty<p class="rounded-2xl border border-dashed border-stone-300 p-10 text-center text-slate-500">No explicit country rules. Streams currently use provider availability.</p>@endforelse</div><div class="mt-6">{{ $rules->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div></div>
