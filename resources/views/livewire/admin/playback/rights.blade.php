<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-emerald-700">Sources & playback · Policy</p><h2 class="mt-2 text-3xl font-extrabold">Rights verification</h2><p class="mt-2 max-w-2xl text-sm text-slate-500">Record distribution review separately from stream health. Rejected source rights prevent that provider stream from starting.</p></div><div class="mt-6 grid gap-3 rounded-2xl border border-stone-200 bg-white p-3 sm:grid-cols-[1fr_180px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search media"><select wire:model.live="status" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="all">All reviews</option><option value="pending">Pending</option><option value="approved">Approved</option><option value="rejected">Rejected</option></select></div><div class="mt-5 grid gap-3">@forelse($sources as $source)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex flex-col gap-4 lg:flex-row lg:items-center"><div class="min-w-0 flex-1"><div class="flex flex-wrap gap-2"><strong>{{ $source->media?->name }}</strong><span class="rounded-full bg-stone-100 px-2 py-1 text-[10px] font-bold">{{ str($source->rightsReview?->status ?? 'pending')->headline() }}</span></div><p class="mt-1 text-xs text-slate-500">{{ $source->sourceProvider?->name }} · {{ $source->media?->country?->name ?? 'Worldwide' }} · Synced {{ $source->last_synchronized_at?->diffForHumans() ?? 'never' }}</p><a href="{{ $source->source_url }}" target="_blank" rel="noopener" class="mt-2 block truncate text-xs font-bold text-blue-700">Inspect source ↗</a></div><input wire:model="notes.{{ $source->id }}" class="min-h-11 rounded-xl border-stone-300 lg:w-72" placeholder="Internal review note"><div class="flex gap-2"><button wire:click="review({{ $source->id }},'approved')" class="rounded-xl bg-emerald-600 px-3 py-3 text-xs font-bold text-white">Approve</button><button wire:click="review({{ $source->id }},'rejected')" wire:confirm="Restrict playback from this source?" class="rounded-xl bg-rose-50 px-3 py-3 text-xs font-bold text-rose-700">Reject</button><button wire:click="review({{ $source->id }},'pending')" class="rounded-xl bg-stone-100 px-3 py-3 text-xs font-bold">Pending</button></div></div></article>@empty<p class="p-10 text-center text-slate-500">No source records match this review.</p>@endforelse</div><div class="mt-6">{{ $sources->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div></div>
