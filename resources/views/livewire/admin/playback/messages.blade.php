<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-blue-700">Sources & playback · Policy</p><h2 class="mt-2 text-3xl font-extrabold">Playback messages</h2><p class="mt-2 text-sm text-slate-500">Maintain concise audience-facing explanations for policy and playback states.</p></div>@if(session('status'))<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif<form wire:submit="save" class="mt-7 grid gap-3">@foreach($records as $record)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex items-center justify-between gap-3"><div><small class="font-extrabold uppercase tracking-wider text-blue-700">{{ $record->key }}</small><h3 class="mt-1 font-extrabold">{{ $record->label }}</h3></div><label class="flex items-center gap-2 text-xs font-bold"><input type="checkbox" wire:model="messages.{{ $record->id }}.is_active"> Active</label></div><textarea wire:model="messages.{{ $record->id }}.message" maxlength="500" rows="2" class="mt-4 w-full rounded-xl border-stone-300 bg-stone-50"></textarea></article>@endforeach<button class="min-h-12 rounded-xl bg-slate-950 px-6 text-sm font-extrabold text-white sm:justify-self-start">Save playback messages</button></form></div>
