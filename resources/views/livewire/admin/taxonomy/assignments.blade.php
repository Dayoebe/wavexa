<div>
    <div><a wire:navigate href="{{ route('admin.taxonomy.'.$kind) }}" class="text-xs font-extrabold text-fuchsia-700">← Back to {{ $kind }}</a><p class="mt-5 text-[10px] font-extrabold uppercase tracking-[.2em] text-fuchsia-700">Discovery · {{ str($kind)->singular()->headline() }}</p><h2 class="mt-2 text-3xl font-extrabold">{{ $taxonomy->name }}</h2><p class="mt-2 text-sm text-slate-500">Media currently assigned to this {{ str($kind)->singular() }}.</p></div>
    <div class="mt-6 grid grid-cols-2 gap-3 sm:grid-cols-4"><button wire:click="$set('type','all')" class="rounded-2xl border p-4 text-left {{ $type==='all'?'border-fuchsia-500 bg-fuchsia-50':'border-stone-200 bg-white' }}"><small class="font-extrabold uppercase text-slate-400">All media</small><strong class="mt-2 block text-2xl">{{ number_format($counts->sum()) }}</strong></button>@foreach(['radio'=>'Radio','tv'=>'Television','podcast'=>'Podcasts'] as $value=>$label)<button wire:click="$set('type','{{ $value }}')" class="rounded-2xl border p-4 text-left {{ $type===$value?'border-fuchsia-500 bg-fuchsia-50':'border-stone-200 bg-white' }}"><small class="font-extrabold uppercase text-slate-400">{{ $label }}</small><strong class="mt-2 block text-2xl">{{ number_format($counts[$value]??0) }}</strong></button>@endforeach</div>
    <label class="mt-5 block rounded-2xl border border-stone-200 bg-white p-2"><span class="sr-only">Search assigned media</span><input wire:model.live.debounce.300ms="q" class="min-h-12 w-full rounded-xl border-0 bg-stone-100 px-4" placeholder="Search assigned stations, channels, or podcasts"></label>
    <div class="mt-5 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[820px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Media</th><th class="p-4">Type</th><th class="p-4">Country</th><th class="p-4">Status</th><th class="p-4">Stream</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($media as $item)<tr><td class="p-4"><strong>{{ $item->name }}</strong><small class="block text-slate-400">{{ $item->radioStation?->call_sign ?: ($item->tvChannel?->call_sign ?: $item->podcast?->author) }}</small></td><td class="p-4 font-bold">{{ $item->type->value==='tv'?'Television':str($item->type->value)->headline() }}</td><td class="p-4">{{ $item->country?->name ?? 'Global' }}</td><td class="p-4"><span class="rounded-full bg-stone-100 px-2.5 py-1 text-[9px] font-extrabold uppercase">{{ $item->status->value }}</span></td><td class="p-4 text-xs">{{ $item->primaryStream?->format ? strtoupper($item->primaryStream->format) : 'No stream' }}</td><td class="p-4 text-right">@if($item->type->value==='radio')<a wire:navigate href="{{ route('admin.radio.show',$item) }}" class="rounded-lg bg-orange-100 px-3 py-2 text-xs font-bold text-orange-800">Manage</a>@elseif($item->type->value==='tv')<a wire:navigate href="{{ route('admin.television.show',$item) }}" class="rounded-lg bg-cyan-100 px-3 py-2 text-xs font-bold text-cyan-800">Manage</a>@elseif($item->type->value==='podcast')<a href="{{ route('podcasts.show',$item->slug) }}" target="_blank" class="rounded-lg bg-amber-100 px-3 py-2 text-xs font-bold text-amber-900">Open ↗</a>@endif</td></tr>@empty<tr><td colspan="6" class="p-12 text-center"><h3 class="text-xl font-extrabold">No assigned media found</h3></td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $media->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div>
</div>
