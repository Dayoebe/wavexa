<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif @error('selected')<div class="mb-5 rounded-2xl bg-rose-100 px-4 py-3 text-sm font-bold text-rose-900">{{ $message }}</div>@enderror
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-rose-700">Discovery · Taxonomy</p><h2 class="mt-2 text-3xl font-extrabold">Tag cleanup</h2><p class="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Review frequencies, URLs, slogans, overly long phrases, and other provider tags that should not be treated as genres.</p></div><button wire:click="cleanAll" wire:confirm="Detach and delete every tag currently identified as noisy?" class="min-h-12 rounded-xl bg-rose-700 px-5 text-sm font-extrabold text-white">Clean all detected</button></div>
    <div class="mt-6 flex flex-col gap-2 rounded-2xl border border-stone-200 bg-white p-2 sm:flex-row"><input wire:model.live.debounce.300ms="q" class="min-h-12 flex-1 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search detected tags"><button wire:click="cleanSelected" wire:confirm="Detach and delete the selected tags?" class="min-h-12 rounded-xl bg-slate-950 px-5 text-sm font-extrabold text-white">Remove selected ({{ count($selected) }})</button></div>
    <div class="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">@forelse($genres as $genre)<label class="flex cursor-pointer items-start gap-3 rounded-2xl border border-stone-200 bg-white p-4"><input wire:model.live="selected" value="{{ $genre->id }}" type="checkbox" class="mt-1 size-5 rounded border-stone-300 text-rose-600"><span class="min-w-0"><strong class="block break-words">{{ $genre->name }}</strong><small class="mt-1 block text-slate-500">Assigned to {{ number_format($genre->media_count) }} media records</small></span></label>@empty<div class="col-span-full rounded-3xl border border-dashed border-stone-300 p-12 text-center"><h3 class="text-xl font-extrabold">No suspicious tags found</h3><p class="mt-2 text-sm text-slate-500">The current genre catalogue passes the configured noise rules.</p></div>@endforelse</div><div class="mt-6">{{ $genres->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div>
</div>
