<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif @error('delete')<div class="mb-5 rounded-2xl bg-rose-100 px-4 py-3 text-sm font-bold text-rose-900">{{ $message }}</div>@enderror
    <div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-fuchsia-700">Discovery · Taxonomy</p><h2 class="mt-2 text-3xl font-extrabold">{{ str($kind)->headline() }}</h2><p class="mt-2 text-sm text-slate-500">Manage reusable discovery metadata and safely merge duplicate terms.</p></div>
    <div class="mt-6 grid gap-5 xl:grid-cols-[1fr_380px]"><section><label class="block rounded-2xl border border-stone-200 bg-white p-2"><input wire:model.live.debounce.300ms="q" class="min-h-12 w-full rounded-xl border-0 bg-stone-100 px-4" placeholder="Search {{ $kind }}"></label><div class="mt-4 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[620px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Name</th><th class="p-4">Identifier</th><th class="p-4">Media</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($items as $item)<tr><td class="p-4 font-extrabold">{{ $item->name }}</td><td class="p-4 text-xs text-slate-500">{{ $kind==='languages' ? (($item->iso_639_1?:'—').' · '.$item->iso_639_3) : $item->slug }}</td><td class="p-4">{{ number_format($item->media_count) }}</td><td class="p-4"><div class="flex justify-end gap-2"><button wire:click="edit({{ $item->id }})" class="rounded-lg bg-blue-100 px-3 py-2 text-xs font-bold text-blue-800">Edit / merge</button><button wire:click="delete({{ $item->id }})" wire:confirm="Delete this unused term?" class="rounded-lg bg-rose-100 px-3 py-2 text-xs font-bold text-rose-800">Delete</button></div></td></tr>@empty<tr><td colspan="4" class="p-12 text-center font-bold">No terms found.</td></tr>@endforelse</tbody></table></div></div><div class="mt-5">{{ $items->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div></section>
    <form wire:submit="save" class="h-fit rounded-3xl border border-stone-200 bg-white p-5 xl:sticky xl:top-24"><h3 class="text-xl font-extrabold">{{ $editingId?'Edit '.str($kind)->singular():'Add '.str($kind)->singular() }}</h3><div class="mt-5 space-y-4"><label class="block text-sm font-bold">Name<input wire:model.live="name" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3">@error('name')<small class="text-rose-700">{{ $message }}</small>@enderror</label>@if($kind!=='languages')<label class="block text-sm font-bold">Slug<input wire:model="slug" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3">@error('slug')<small class="text-rose-700">{{ $message }}</small>@enderror</label>@endif @if($kind==='categories')<label class="block text-sm font-bold">Description<textarea wire:model="description" rows="4" class="mt-2 w-full rounded-xl border border-stone-300 p-3"></textarea></label>@endif @if($kind==='languages')<div class="grid grid-cols-2 gap-3"><label class="text-sm font-bold">ISO 639-1<input wire:model="iso6391" maxlength="2" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3 lowercase">@error('iso6391')<small class="text-rose-700">{{ $message }}</small>@enderror</label><label class="text-sm font-bold">ISO 639-3<input wire:model="iso6393" maxlength="3" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3 lowercase">@error('iso6393')<small class="text-rose-700">{{ $message }}</small>@enderror</label></div>@endif</div><div class="mt-5 flex gap-2"><button class="min-h-11 flex-1 rounded-xl bg-fuchsia-700 px-4 text-sm font-extrabold text-white">Save</button>@if($editingId)<button type="button" wire:click="cancel" class="rounded-xl border border-stone-300 px-4 text-sm font-bold">Cancel</button>@endif</div>@if($editingId)<div class="mt-6 border-t border-stone-200 pt-5"><p class="text-xs font-extrabold uppercase tracking-wider text-slate-500">Merge this term</p><p class="mt-2 text-xs leading-5 text-slate-500">All media assignments move to the selected term; this term is then removed.</p><select wire:model="mergeTargetId" class="mt-3 min-h-11 w-full rounded-xl border border-stone-300 px-3"><option value="">Select destination</option>@foreach($targets as $target)<option value="{{ $target->id }}">{{ $target->name }}</option>@endforeach</select>@error('mergeTargetId')<small class="text-rose-700">{{ $message }}</small>@enderror<button type="button" wire:click="merge" wire:confirm="Merge this term and move all media assignments?" class="mt-3 min-h-11 w-full rounded-xl bg-amber-100 px-4 text-sm font-extrabold text-amber-900">Merge into destination</button></div>@endif</form></div>
</div>
