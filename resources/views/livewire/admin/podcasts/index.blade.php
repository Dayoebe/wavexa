<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-amber-700">Media catalogue</p><h2 class="mt-2 text-3xl font-extrabold">Podcasts</h2><p class="mt-2 text-sm text-slate-500">Review synchronized shows, publisher feeds, episodes, and public playback.</p></div><a wire:navigate href="{{ route('admin.podcasts.create') }}" class="grid min-h-12 place-items-center rounded-xl bg-amber-600 px-5 text-sm font-extrabold text-white">＋ Add podcast</a></div>
    <div class="mt-7 grid grid-cols-3 gap-3">@foreach($stats as $label=>$value)<article class="rounded-2xl border border-stone-200 bg-white p-4"><p class="text-[9px] font-extrabold uppercase tracking-wider text-slate-400">{{ $label }}</p><p class="mt-2 text-3xl font-extrabold">{{ number_format($value) }}</p></article>@endforeach</div>
    <label class="mt-6 block rounded-2xl border border-stone-200 bg-white p-2"><span class="sr-only">Search podcasts</span><input wire:model.live.debounce.300ms="q" class="min-h-12 w-full rounded-xl border-0 bg-stone-100 px-4 outline-none" placeholder="Search podcast or publisher"></label>
    <div class="mt-6 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[820px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Show</th><th class="p-4">Country</th><th class="p-4">Episodes</th><th class="p-4">Last fetched</th><th class="p-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($podcasts as $item)<tr><td class="p-4"><strong class="block">{{ $item->name }}</strong><small class="text-slate-400">{{ $item->podcast?->author ?: 'Unknown publisher' }}</small></td><td class="p-4">{{ $item->country?->name ?? 'Not assigned' }}</td><td class="p-4 font-bold">{{ number_format($item->podcast_episodes_count) }}</td><td class="p-4 text-xs text-slate-500">{{ $item->podcast?->last_fetched_at?->diffForHumans() ?: 'Never' }}</td><td class="p-4"><div class="flex justify-end gap-2"><a href="{{ route('podcasts.show',$item->slug) }}" target="_blank" class="rounded-lg bg-amber-100 px-3 py-2 text-xs font-bold text-amber-900">Open ↗</a><button wire:click="delete({{ $item->id }})" wire:confirm="Remove this podcast from Wavexa?" class="rounded-lg bg-rose-100 px-3 py-2 text-xs font-bold text-rose-800">Delete</button></div></td></tr>@empty<tr><td colspan="5" class="p-12 text-center"><h3 class="text-xl font-extrabold">No podcasts found</h3></td></tr>@endforelse</tbody></table></div></div>
    <div class="mt-6">{{ $podcasts->onEachSide(1)->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div>
</div>
