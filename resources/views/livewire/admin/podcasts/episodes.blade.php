<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif
    <div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-violet-700">Podcast catalogue</p><h2 class="mt-2 text-3xl font-extrabold">Episodes</h2><p class="mt-2 text-sm text-slate-500">Search, inspect, play, and moderate imported audio and video episodes.</p></div>
    <div class="mt-6 grid gap-2 rounded-2xl border border-stone-200 bg-white p-2 sm:grid-cols-[1fr_180px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search episode title"><select wire:model.live="format" class="min-h-12 rounded-xl border-0 bg-stone-100 px-3 font-bold"><option value="all">All formats</option><option value="audio">Audio</option><option value="video">Video</option></select></div>
    <div class="mt-6 space-y-3">@forelse($episodes as $episode)@php($show=$episode->podcastEpisode?->podcast?->media)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex flex-col gap-4 sm:flex-row sm:items-center"><div class="min-w-0 flex-1"><p class="text-[9px] font-extrabold uppercase tracking-wider text-violet-700">{{ in_array($episode->primaryStream?->format,['mp4','webm'],true)?'Video':'Audio' }} · {{ $episode->podcastEpisode?->published_at?->format('M j, Y') ?: 'No date' }}</p><h3 class="mt-1 truncate font-extrabold">{{ $episode->name }}</h3><p class="mt-1 truncate text-xs text-slate-500">{{ $show?->name ?: 'Unknown podcast' }}</p></div><div class="flex gap-2">@if($show)<a href="{{ route('podcasts.show',$show->slug) }}" target="_blank" class="rounded-lg bg-amber-100 px-3 py-2 text-xs font-bold text-amber-900">Play ↗</a>@endif<button wire:click="delete({{ $episode->id }})" wire:confirm="Remove this episode from Wavexa?" class="rounded-lg bg-rose-100 px-3 py-2 text-xs font-bold text-rose-800">Delete</button></div></div></article>@empty<div class="rounded-3xl border border-dashed border-stone-300 p-12 text-center"><h3 class="text-xl font-extrabold">No episodes found</h3></div>@endforelse</div>
    <div class="mt-6">{{ $episodes->onEachSide(1)->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div>
</div>
