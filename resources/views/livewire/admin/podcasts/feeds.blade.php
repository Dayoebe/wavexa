<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif @error('sync')<div class="mb-5 rounded-2xl bg-rose-100 px-4 py-3 text-sm font-bold text-rose-900">{{ $message }}</div>@enderror
    <div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-emerald-700">Podcast ingestion</p><h2 class="mt-2 text-3xl font-extrabold">RSS feeds</h2><p class="mt-2 text-sm text-slate-500">Monitor publisher feeds and synchronize their latest episodes.</p></div><a wire:navigate href="{{ route('admin.podcasts.create') }}" class="grid min-h-12 place-items-center rounded-xl bg-amber-600 px-5 text-sm font-extrabold text-white">＋ Add feed</a></div>
    <label class="mt-6 block rounded-2xl border border-stone-200 bg-white p-2"><span class="sr-only">Search feeds</span><input wire:model.live.debounce.300ms="q" class="min-h-12 w-full rounded-xl border-0 bg-stone-100 px-4" placeholder="Search podcast feeds"></label>
    <div class="mt-6 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[860px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Podcast</th><th class="p-4">Feed URL</th><th class="p-4">Episodes</th><th class="p-4">Last synchronized</th><th class="p-4 text-right">Action</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($feeds as $item)<tr><td class="p-4"><strong>{{ $item->name }}</strong><small class="block text-slate-400">{{ $item->country?->name ?? 'Global' }}</small></td><td class="max-w-72 truncate p-4 text-xs text-slate-500" title="{{ $item->podcast->feed_url }}">{{ $item->podcast->feed_url }}</td><td class="p-4 font-bold">{{ number_format($item->podcast_episodes_count) }}</td><td class="p-4 text-xs text-slate-500">{{ $item->podcast->last_fetched_at?->diffForHumans() ?: 'Never' }}</td><td class="p-4 text-right"><button wire:click="sync({{ $item->id }})" wire:loading.attr="disabled" class="rounded-lg bg-emerald-100 px-3 py-2 text-xs font-bold text-emerald-900">Synchronize</button></td></tr>@empty<tr><td colspan="5" class="p-12 text-center"><h3 class="text-xl font-extrabold">No RSS feeds found</h3></td></tr>@endforelse</tbody></table></div></div>
    <div class="mt-6">{{ $feeds->onEachSide(1)->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div>
</div>
