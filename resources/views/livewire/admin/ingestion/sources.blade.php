<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-orange-700">Sources & playback · Ingestion</p><h2 class="mt-2 text-3xl font-extrabold">Source providers</h2><p class="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Control trusted catalogue directories and inspect the provenance they contribute. Disabling a provider prevents new dashboard imports without deleting existing media.</p></div>
<div class="mt-7 grid gap-4 lg:grid-cols-3">@forelse($providers as $provider)<article class="rounded-3xl border border-stone-200 bg-white p-5"><div class="flex items-start justify-between gap-4"><div><span class="inline-flex rounded-full px-2.5 py-1 text-[10px] font-extrabold uppercase {{ $provider->is_active ? 'bg-emerald-50 text-emerald-700' : 'bg-stone-100 text-slate-500' }}">{{ $provider->is_active ? 'Active' : 'Disabled' }}</span><h3 class="mt-3 text-xl font-extrabold">{{ $provider->name }}</h3></div><button wire:click="toggle({{ $provider->id }})" class="rounded-xl px-3 py-2 text-xs font-bold {{ $provider->is_active ? 'bg-rose-50 text-rose-700' : 'bg-emerald-600 text-white' }}">{{ $provider->is_active ? 'Disable' : 'Enable' }}</button></div><a href="{{ $provider->website_url }}" target="_blank" rel="noopener noreferrer" class="mt-2 block truncate text-xs font-bold text-blue-700">{{ $provider->website_url }} ↗</a><div class="mt-5 grid grid-cols-3 gap-2 text-center"><div class="rounded-xl bg-orange-50 p-3"><strong class="block text-lg">{{ number_format($provider->media_sources_count) }}</strong><small class="text-[9px] uppercase text-slate-500">Media</small></div><div class="rounded-xl bg-cyan-50 p-3"><strong class="block text-lg">{{ number_format($provider->stream_sources_count) }}</strong><small class="text-[9px] uppercase text-slate-500">Streams</small></div><div class="rounded-xl bg-violet-50 p-3"><strong class="block text-lg">{{ number_format($provider->ingestion_runs_count) }}</strong><small class="text-[9px] uppercase text-slate-500">Runs</small></div></div></article>@empty<p class="rounded-3xl border border-dashed border-stone-300 p-10 text-slate-500">Providers appear here after an import is configured or run.</p>@endforelse</div></div>
