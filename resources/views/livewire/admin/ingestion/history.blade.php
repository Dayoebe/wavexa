<div wire:poll.10s><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-orange-700">Sources & playback · Ingestion</p><h2 class="mt-2 text-3xl font-extrabold">Import history</h2><p class="mt-2 text-sm text-slate-500">Audit every queued catalogue synchronization, its options, outcome, and operator.</p></div>
@if(session('status'))<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif @error('source')<div class="mt-5 rounded-2xl bg-rose-50 p-4 text-sm font-bold text-rose-800">{{ $message }}</div>@enderror<div class="mt-6 grid grid-cols-2 gap-3 lg:grid-cols-4">@foreach($stats as $label => $value)<article class="rounded-2xl border border-stone-200 bg-white p-4"><p class="text-[9px] font-extrabold uppercase tracking-wider text-slate-400">{{ $label }}</p><p class="mt-2 text-3xl font-extrabold">{{ number_format($value) }}</p></article>@endforeach</div>
<div class="mt-5 grid gap-3 rounded-2xl border border-stone-200 bg-white p-3 sm:grid-cols-2"><select wire:model.live="type" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="all">All media types</option><option value="radio">Radio</option><option value="tv">Television</option><option value="podcast">Podcasts</option></select><select wire:model.live="status" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="all">All statuses</option><option value="queued">Queued</option><option value="running">Running</option><option value="completed">Completed</option><option value="failed">Failed</option></select></div>
<div class="mt-5 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Run</th><th class="p-4">Source</th><th class="p-4">Status</th><th class="p-4">Result</th><th class="p-4">Operator</th><th class="p-4">Started</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($runs as $run)<tr><td class="p-4"><strong>#{{ $run->id }}</strong><small class="ml-2 uppercase text-slate-500">{{ $run->type }}</small></td><td class="p-4">{{ $run->sourceProvider?->name ?? 'Removed source' }}</td><td class="p-4"><span class="rounded-full px-2 py-1 text-xs font-bold {{ $run->status === 'completed' ? 'bg-emerald-50 text-emerald-700' : ($run->status === 'failed' ? 'bg-rose-50 text-rose-700' : 'bg-amber-50 text-amber-700') }}">{{ str($run->status)->headline() }}</span>@if($run->error_message)<p class="mt-2 max-w-xs truncate text-xs text-rose-700" title="{{ $run->error_message }}">{{ $run->error_message }}</p>@endif</td><td class="p-4 text-xs">@forelse($run->result ?? [] as $label => $value)<span class="mr-2 whitespace-nowrap">{{ str($label)->headline() }}: <b>{{ $value }}</b></span>@empty—@endforelse</td><td class="p-4 text-xs">{{ $run->requestedBy?->email ?? 'System' }}</td><td class="p-4 text-xs text-slate-500">{{ ($run->started_at ?? $run->created_at)->diffForHumans() }}</td><td class="p-4 text-right"><button wire:click="retry({{ $run->id }})" class="rounded-lg bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700">Run again</button></td></tr>@empty<tr><td colspan="7" class="p-12 text-center text-slate-500">No import runs match these filters.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $runs->links('livewire.components.pagination', data: ['scrollTo' => 'main']) }}</div></div>
