<div>
    <div class="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-orange-600">Playback operations</p><h2 class="mt-2 text-3xl font-extrabold">Stream health</h2><p class="mt-2 text-sm text-slate-500">Healthy, failing, unverified, and user-reported sources.</p></div><span class="text-xs font-semibold text-slate-400">Checks run through the queue</span></div>
    <div class="mt-7 grid grid-cols-2 gap-3 lg:grid-cols-5">@foreach ($summary as $label => $value)<div class="rounded-2xl border border-stone-200 bg-white p-4"><p class="text-[10px] font-extrabold uppercase tracking-wider text-slate-400">{{ $label }}</p><p class="mt-3 text-3xl font-extrabold">{{ number_format($value) }}</p></div>@endforeach</div>
    <div class="mt-7 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[760px] text-left text-sm"><thead class="bg-stone-100 text-[10px] uppercase tracking-wider text-slate-500"><tr><th class="p-4">Media</th><th class="p-4">Status</th><th class="p-4">Response</th><th class="p-4">Failures</th><th class="p-4">Last check</th><th class="p-4">Reports</th></tr></thead><tbody class="divide-y divide-stone-100">@forelse ($streams as $stream)<tr><td class="p-4 font-bold">{{ $stream->media?->name }}<span class="block text-xs font-normal text-slate-400">{{ $stream->media?->country?->name }}</span></td><td class="p-4"><span class="rounded-full px-2.5 py-1 text-[10px] font-extrabold {{ $stream->status->value === 'online' ? 'bg-emerald-100 text-emerald-800' : ($stream->status->value === 'offline' ? 'bg-rose-100 text-rose-800' : 'bg-stone-100 text-slate-600') }}">{{ $stream->status->value }}</span></td><td class="p-4">{{ $stream->http_status ?? '—' }} · {{ $stream->response_time_ms ? $stream->response_time_ms.'ms' : '—' }}</td><td class="p-4">{{ $stream->failure_count }}</td><td class="p-4">{{ $stream->last_checked_at?->diffForHumans() ?? 'Never' }}</td><td class="p-4">{{ $stream->reports->whereNull('resolved_at')->count() }}</td></tr>@empty<tr><td colspan="6" class="p-10 text-center text-slate-500">No stream sources found.</td></tr>@endforelse</tbody></table></div></div>
    <div class="mt-6">{{ $streams->onEachSide(1)->links('livewire.components.pagination') }}</div>
</div>
