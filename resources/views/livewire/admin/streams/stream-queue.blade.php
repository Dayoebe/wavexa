<div><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-amber-700">Sources & playback · Stream operations</p><h2 class="mt-2 text-3xl font-extrabold">{{ str($kind)->headline() }} streams</h2><p class="mt-2 max-w-2xl text-sm text-slate-500">{{ $kind === 'offline' ? 'Review sources that reached the repeated-failure threshold and queue fresh technical checks.' : 'Review provider provenance separately from technical availability before approving a source.' }}</p></div><button wire:click="checkBatch" wire:loading.attr="disabled" class="min-h-11 rounded-xl bg-slate-950 px-4 text-sm font-extrabold text-white disabled:opacity-50">Check up to 100</button></div>
@if(session('status'))<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif<div class="mt-6 grid gap-3 rounded-2xl border border-stone-200 bg-white p-3 sm:grid-cols-[1fr_180px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search media"><select wire:model.live="type" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="all">All types</option><option value="radio">Radio</option><option value="tv">Television</option><option value="podcast_episode">Podcast episodes</option></select></div>
<div class="mt-5 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[850px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Media</th><th class="p-4">Provider</th><th class="p-4">Technical status</th><th class="p-4">Verification</th><th class="p-4">Failures</th><th class="p-4">Last check</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($streams as $stream)<tr><td class="p-4"><strong>{{ $stream->media?->name }}</strong><small class="block text-slate-400">{{ $stream->media?->country?->name ?? 'Worldwide' }} · {{ strtoupper($stream->format) }}</small></td><td class="p-4">{{ $stream->sourceProvider?->name ?? 'Manual' }}</td><td class="p-4">{{ str($stream->status->value)->headline() }}@if($stream->failure_reason)<small class="mt-1 block max-w-xs truncate text-rose-600" title="{{ $stream->failure_reason }}">{{ $stream->failure_reason }}</small>@endif</td><td class="p-4">{{ str($stream->verification_status->value)->headline() }}</td><td class="p-4 font-bold">{{ $stream->failure_count }}</td><td class="p-4 text-xs text-slate-500">{{ $stream->last_checked_at?->diffForHumans() ?? 'Never' }}</td><td class="p-4"><div class="flex justify-end gap-2"><button wire:click="recheck({{ $stream->id }})" class="rounded-lg bg-blue-50 px-3 py-2 text-xs font-bold text-blue-700">Check</button>@if($kind === 'unverified')<button wire:click="verify({{ $stream->id }})" class="rounded-lg bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700">Verify</button><button wire:click="reject({{ $stream->id }})" wire:confirm="Reject this source?" class="rounded-lg bg-rose-50 px-3 py-2 text-xs font-bold text-rose-700">Reject</button>@endif</div></td></tr>@empty<tr><td colspan="7" class="p-12 text-center text-slate-500">This operations queue is clear.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $streams->links('livewire.components.pagination', data: ['scrollTo' => 'main']) }}</div></div>
