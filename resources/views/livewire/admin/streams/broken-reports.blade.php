<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-rose-700">Sources & playback · Stream operations</p><h2 class="mt-2 text-3xl font-extrabold">Broken stream reports</h2><p class="mt-2 text-sm text-slate-500">Triage playback problems submitted by listeners and viewers without exposing reporter identifiers.</p></div>
@if(session('status'))<div class="mt-5 rounded-2xl bg-emerald-50 p-4 text-sm font-bold text-emerald-800">{{ session('status') }}</div>@endif<div class="mt-6 grid gap-3 sm:grid-cols-2"><article class="rounded-2xl border border-stone-200 bg-white p-4"><small class="font-extrabold uppercase text-slate-400">Open reports</small><strong class="mt-2 block text-3xl">{{ number_format($counts['open']) }}</strong></article><article class="rounded-2xl border border-stone-200 bg-white p-4"><small class="font-extrabold uppercase text-slate-400">Resolved</small><strong class="mt-2 block text-3xl">{{ number_format($counts['resolved']) }}</strong></article></div>
<div class="mt-5 grid gap-3 rounded-2xl border border-stone-200 bg-white p-3 sm:grid-cols-[1fr_180px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search station or channel"><select wire:model.live="status" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="open">Open reports</option><option value="resolved">Resolved</option><option value="all">All reports</option></select></div>
<div class="mt-5 grid gap-3">@forelse($reports as $report)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex flex-col gap-4 sm:flex-row sm:items-center"><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><strong class="truncate">{{ $report->streamSource?->media?->name ?? 'Removed media' }}</strong><span class="rounded-full bg-rose-50 px-2 py-1 text-[10px] font-extrabold uppercase text-rose-700">{{ str($report->reason)->headline() }}</span></div><p class="mt-2 text-sm text-slate-600">{{ $report->details ?: 'No additional details supplied.' }}</p><p class="mt-2 text-xs text-slate-400">{{ $report->streamSource?->media?->country?->name ?? 'Worldwide' }} · Reported {{ $report->created_at->diffForHumans() }} · Stream status: {{ $report->streamSource?->status?->value ?? 'removed' }}</p></div><div class="flex gap-2"><button wire:click="recheck({{ $report->id }})" class="min-h-10 rounded-xl bg-blue-50 px-3 text-xs font-bold text-blue-700">Recheck</button>@if($report->resolved_at)<button wire:click="reopen({{ $report->id }})" class="min-h-10 rounded-xl bg-stone-100 px-3 text-xs font-bold">Reopen</button>@else<button wire:click="resolve({{ $report->id }})" class="min-h-10 rounded-xl bg-emerald-600 px-3 text-xs font-bold text-white">Resolve</button>@endif</div></div></article>@empty<p class="rounded-2xl border border-dashed border-stone-300 p-10 text-center text-slate-500">No reports match this view.</p>@endforelse</div><div class="mt-6">{{ $reports->links('livewire.components.pagination', data: ['scrollTo' => 'main']) }}</div></div>
