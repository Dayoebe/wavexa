<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-emerald-700">Discovery · Editorial</p><h2 class="mt-2 text-3xl font-extrabold">Popular destinations</h2><p class="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Pin priority countries for the public homepage. Unpinned destinations remain ranked automatically by published catalogue depth.</p></div>
<section class="mt-7 rounded-3xl border border-stone-200 bg-white p-5"><div class="flex items-center justify-between"><h3 class="text-xl font-extrabold">Promoted order</h3><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-800">{{ $promotions->count() }} pinned</span></div><div class="mt-4 grid gap-3">@forelse($promotions as $promotion)<article class="flex items-center gap-3 rounded-2xl border border-stone-200 p-3"><span class="grid size-10 place-items-center rounded-xl bg-emerald-50 font-black text-emerald-700">{{ $promotion->position }}</span><div class="min-w-0 flex-1"><strong>{{ $promotion->country->name }}</strong><small class="ml-2 text-slate-500">{{ $promotion->country->iso_alpha_2 }}</small></div><button wire:click="move({{ $promotion->country_id }}, 'up')" class="grid size-10 place-items-center rounded-xl bg-stone-100">↑</button><button wire:click="move({{ $promotion->country_id }}, 'down')" class="grid size-10 place-items-center rounded-xl bg-stone-100">↓</button><button wire:click="remove({{ $promotion->country_id }})" class="min-h-10 rounded-xl bg-rose-50 px-3 text-xs font-bold text-rose-700">Unpin</button></article>@empty<p class="rounded-2xl border border-dashed border-stone-300 p-8 text-center text-sm text-slate-500">No destinations pinned. Automatic popularity is currently in control.</p>@endforelse</div></section>
<section class="mt-6"><label class="block rounded-2xl border border-stone-200 bg-white p-2"><input wire:model.live.debounce.300ms="q" class="min-h-12 w-full rounded-xl border-0 bg-stone-100 px-4" placeholder="Find a country"></label><div class="mt-4 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">@forelse($countries as $country)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex items-start justify-between gap-3"><div><small class="font-extrabold uppercase tracking-widest text-slate-400">{{ $country->iso_alpha_2 }}</small><h3 class="mt-1 text-lg font-extrabold">{{ $country->name }}</h3></div><button wire:click="promote({{ $country->id }})" @disabled($promotions->contains('country_id', $country->id)) class="rounded-xl bg-slate-950 px-3 py-2 text-xs font-bold text-white disabled:bg-stone-200 disabled:text-slate-400">{{ $promotions->contains('country_id', $country->id) ? 'Pinned' : 'Pin' }}</button></div><p class="mt-4 text-xs text-slate-500">{{ number_format($country->radio_count) }} radio · {{ number_format($country->tv_count) }} TV · {{ number_format($country->podcast_count) }} podcasts</p></article>@empty<p class="text-slate-500">No countries found.</p>@endforelse</div><div class="mt-6">{{ $countries->links('livewire.components.pagination', data: ['scrollTo' => 'main']) }}</div></section></div>
