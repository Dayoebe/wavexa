<div><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-cyan-700">Discovery · Editorial</p><h2 class="mt-2 text-3xl font-extrabold">Recently added</h2><p class="mt-2 text-sm text-slate-500">Review new catalogue arrivals, publication state, and freshness across every media type.</p></div>
<div class="mt-6 grid gap-3 rounded-2xl border border-stone-200 bg-white p-3 md:grid-cols-[1fr_180px_180px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search new media"><select wire:model.live="type" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="all">All media</option><option value="radio">Radio</option><option value="tv">Television</option><option value="podcast">Podcasts</option></select><select wire:model.live="days" class="min-h-12 rounded-xl border-0 bg-stone-100"><option value="7">Last 7 days</option><option value="30">Last 30 days</option><option value="90">Last 90 days</option><option value="all">All time</option></select></div>
<div class="mt-5 overflow-hidden rounded-2xl border border-stone-200 bg-white"><div class="overflow-x-auto"><table class="w-full min-w-[720px] text-left text-sm"><thead class="bg-stone-100 text-[9px] font-extrabold uppercase tracking-wider text-slate-500"><tr><th class="p-4">Media</th><th class="p-4">Type</th><th class="p-4">Country</th><th class="p-4">Status</th><th class="p-4">Added</th><th class="p-4"></th></tr></thead><tbody class="divide-y divide-stone-100">@forelse($media as $item)<tr><td class="p-4 font-extrabold">{{ $item->name }}</td><td class="p-4">{{ str($item->type->value)->headline() }}</td><td class="p-4">{{ $item->country?->name ?? 'Worldwide' }}</td><td class="p-4"><span class="rounded-full bg-stone-100 px-2 py-1 text-xs font-bold">{{ str($item->status->value)->headline() }}</span></td><td class="p-4 text-xs text-slate-500">{{ $item->created_at->diffForHumans() }}</td><td class="p-4 text-right">@if($item->type->value === 'radio')<a href="{{ route('admin.radio.edit', $item) }}" class="font-bold text-blue-700">Manage</a>@elseif($item->type->value === 'tv')<a href="{{ route('admin.television.edit', $item) }}" class="font-bold text-blue-700">Manage</a>@elseif($item->type->value === 'podcast')<a href="{{ route('admin.podcasts.index') }}" class="font-bold text-blue-700">Manage</a>@endif</td></tr>@empty<tr><td colspan="6" class="p-12 text-center text-slate-500">No media added in this period.</td></tr>@endforelse</tbody></table></div></div><div class="mt-6">{{ $media->links('livewire.components.pagination', data: ['scrollTo' => 'main']) }}</div></div>
