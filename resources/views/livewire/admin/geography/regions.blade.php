<div>
    @if(session('success'))<div class="mb-5 rounded-2xl bg-emerald-100 px-4 py-3 text-sm font-bold text-emerald-900">{{ session('success') }}</div>@endif @error('delete')<div class="mb-5 rounded-2xl bg-rose-100 px-4 py-3 text-sm font-bold text-rose-900">{{ $message }}</div>@enderror
    <div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-indigo-700">Discovery · Geography</p><h2 class="mt-2 text-3xl font-extrabold">Regions, states & provinces</h2><p class="mt-2 text-sm text-slate-500">Flexible administrative areas with optional parent areas.</p></div>
    <div class="mt-6 grid gap-5 xl:grid-cols-[1fr_380px]"><section><div class="grid gap-2 rounded-2xl border border-stone-200 bg-white p-2 sm:grid-cols-[1fr_220px]"><input wire:model.live.debounce.300ms="q" class="min-h-12 rounded-xl border-0 bg-stone-100 px-4" placeholder="Search region"><select wire:model.live="countryFilter" class="min-h-12 rounded-xl border-0 bg-stone-100 px-3 font-bold"><option value="">Every country</option>@foreach($countries as $country)<option value="{{ $country->id }}">{{ $country->name }}</option>@endforeach</select></div><div class="mt-4 space-y-3">@forelse($regions as $item)<article class="rounded-2xl border border-stone-200 bg-white p-4"><div class="flex flex-col gap-3 sm:flex-row sm:items-center"><div class="min-w-0 flex-1"><p class="text-[9px] font-extrabold uppercase tracking-wider text-indigo-700">{{ $item->country->name }} · {{ $item->type ?: 'area' }} {{ $item->code?'· '.$item->code:'' }}</p><h3 class="mt-1 font-extrabold">{{ $item->name }}</h3><p class="mt-1 text-xs text-slate-500">{{ $item->parent?->name ? 'Inside '.$item->parent->name.' · ' : '' }}{{ $item->cities_count }} cities · {{ $item->media_count }} media · {{ $item->children_count }} child areas</p></div><div class="flex gap-2"><button wire:click="edit({{ $item->id }})" class="rounded-lg bg-blue-100 px-3 py-2 text-xs font-bold text-blue-800">Edit</button><button wire:click="delete({{ $item->id }})" wire:confirm="Delete this region?" class="rounded-lg bg-rose-100 px-3 py-2 text-xs font-bold text-rose-800">Delete</button></div></div></article>@empty<div class="rounded-3xl border border-dashed border-stone-300 p-12 text-center font-bold">No regions found.</div>@endforelse</div><div class="mt-5">{{ $regions->links('livewire.components.pagination',data:['scrollTo'=>'main']) }}</div></section>
    <form wire:submit="save" class="h-fit rounded-3xl border border-stone-200 bg-white p-5 xl:sticky xl:top-24"><h3 class="text-xl font-extrabold">{{ $editingId?'Edit region':'Add region' }}</h3><div class="mt-5 space-y-4"><label class="block text-sm font-bold">Country<select wire:model.live="countryId" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3"><option value="">Select country</option>@foreach($countries as $country)<option value="{{ $country->id }}">{{ $country->name }}</option>@endforeach</select>@error('countryId')<small class="text-rose-700">{{ $message }}</small>@enderror</label><label class="block text-sm font-bold">Name<input wire:model="name" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3">@error('name')<small class="text-rose-700">{{ $message }}</small>@enderror</label><div class="grid grid-cols-2 gap-3"><label class="text-sm font-bold">Code<input wire:model="code" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3"></label><label class="text-sm font-bold">Type<input wire:model="type" placeholder="state" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3"></label></div><label class="block text-sm font-bold">Parent area <span class="font-normal text-slate-400">optional</span><select wire:model="parentId" class="mt-2 min-h-11 w-full rounded-xl border border-stone-300 px-3"><option value="">No parent</option>@foreach($parents as $parent)<option value="{{ $parent->id }}">{{ $parent->name }}</option>@endforeach</select>@error('parentId')<small class="text-rose-700">{{ $message }}</small>@enderror</label></div><div class="mt-5 flex gap-2"><button class="min-h-11 flex-1 rounded-xl bg-indigo-700 px-4 text-sm font-extrabold text-white">Save region</button>@if($editingId)<button type="button" wire:click="cancel" class="rounded-xl border border-stone-300 px-4 text-sm font-bold">Cancel</button>@endif</div></form></div>
</div>
