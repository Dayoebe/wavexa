@extends('layouts.public')
@section('title', $country->name.' live radio and TV — Wavexa')
@section('description', 'Discover live radio stations and television channels from '.$country->name.' on Wavexa.')
@section('content')
<section class="border-b border-violet-200 bg-violet-50"><div class="mx-auto max-w-7xl px-4 py-10 sm:px-6 sm:py-16 lg:px-8"><a href="{{ route('home') }}" class="text-sm font-bold text-violet-700">← Global discovery</a><p class="mt-8 text-xs font-extrabold uppercase tracking-[0.18em] text-violet-700">Country guide</p><h1 class="mt-3 text-4xl font-extrabold tracking-[-0.05em] sm:text-6xl">Live from {{ $country->name }}</h1><p class="mt-4 max-w-2xl text-slate-600">Explore directory-listed radio and supported live television from one place.</p></div></section>
<section class="py-10"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="flex items-end justify-between"><div><p class="text-xs font-extrabold uppercase tracking-wider text-orange-600">Listen</p><h2 class="mt-2 text-2xl font-extrabold">Radio stations</h2></div><a href="{{ route('radio.index', ['country'=>$country->iso_alpha_2]) }}" class="text-sm font-bold text-orange-700">View all</a></div><div class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">@forelse($radioStations as $media)<a href="{{ route('radio.show',$media->slug) }}" class="rounded-2xl border border-stone-200 bg-white p-4 font-bold shadow-sm">{{ $media->name }}<span class="mt-1 block text-xs font-normal text-slate-500">Live radio</span></a>@empty<p class="text-slate-500">No radio stations available yet.</p>@endforelse</div></div></section>
<section class="border-t border-cyan-200 bg-cyan-50 py-10"><div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8"><div class="flex items-end justify-between"><div><p class="text-xs font-extrabold uppercase tracking-wider text-cyan-700">Watch</p><h2 class="mt-2 text-2xl font-extrabold">Television channels</h2></div><a href="{{ route('tv.index', ['country'=>$country->iso_alpha_2]) }}" class="text-sm font-bold text-cyan-800">View all</a></div><div class="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">@forelse($tvChannels as $media)<a href="{{ route('tv.show',$media->slug) }}" class="rounded-2xl border border-cyan-200 bg-white p-4 font-bold shadow-sm">{{ $media->name }}<span class="mt-1 block text-xs font-normal text-slate-500">Live TV</span></a>@empty<p class="text-slate-500">No television channels available yet.</p>@endforelse</div></div></section>
@endsection
